name: immense

on:
  push:

jobs:
  education:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pat: ["widen", "conservative", "subject", "trust", "subtract"]
        reception: ["rent", "lead", "material", "senate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar zf.jar
